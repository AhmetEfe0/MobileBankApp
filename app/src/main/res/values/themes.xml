<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjeDeneme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/light_background</item>
        <item name="android:windowBackground">#E4E2E2</item>
    </style>

    <style name="Theme.ProjeDeneme" parent="Base.Theme.ProjeDeneme" />
</resources>